namespace SevicesV2
{
    public class Cabinet<T>

    {
        // a list of concreate variable or an array
        //OR A LIST(LIKE IN JAVA) - NO LONGER _COUNT
         //T: Type, can pass a data tuupe

        //CRUD METHODS NORMAL
        List <T> list = new List <T> ();

        //Aray Líst <Student> _list = new Arraylist 
              

        //không cần biến count vì giỏ co giản như túi doraemon 
        //đc quyền for đến hết vì mảng cấp theo nh ucaauf thực sự dùng 
        //bên java .Size() trả về số phần tử đg có 
        //bên c#  .Count{get;} giống .Length của mảng 




    }
}
